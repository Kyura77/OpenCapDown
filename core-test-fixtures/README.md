# Módulo `core-test-fixtures`

Fakes e stubs compartilhados entre testes do `core`.

## Regras
- Depende apenas de `:core` e `mockk`.
- Usado como `testImplementation(project(":core-test-fixtures"))`.
